<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools">

    <application
        android:allowBackup="true"
        android:dataExtractionRules="@xml/data_extraction_rules"
        android:fullBackupContent="@xml/backup_rules"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:supportsRtl="true"
        android:enableOnBackInvokedCallback="true"
        android:theme="@style/Theme.LoginDemoApp"
        tools:targetApi="31">

        <activity
            android:name=".view.activity.ActivityMain"
            android:exported="false" />
        <activity
            android:name=".view.activity.ActivitySignup"
            android:exported="false"
            android:parentActivityName=".view.activity.ActivityLogin"/>
        <activity
            android:name=".view.activity.ActivityLogin"
            android:exported="true"
            android:theme="@style/loginTheme">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".view.activity.ActivityProfile"
            android:exported="false"
            />

        <activity android:name=".view.activity.ActivityPhotos"
            android:exported="false"
            />
    </application>
    <uses-permission android:name="android.permission.READ_EXTERNAL_STORAGE" />

</manifest>